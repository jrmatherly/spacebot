# Spacebot - Project Overview

**Version:** 0.5.1
**Language:** Rust (edition 2024), ~130K lines of code
**Purpose:** An agentic system where every LLM process has a dedicated role. Replaces the monolithic session model with specialized processes (Channel, Branch, Worker, Compactor, Cortex).

## Tech Stack
- **Runtime:** Tokio async
- **HTTP Framework:** Axum 0.8
- **Database:** SQLite via sqlx 0.8 (55 migrations total: 41 in flat per-agent `migrations/`, 14 in instance-wide `migrations/global/` — 5 added by Phase 2 on 2026-04-21, `users_photo` added by Phase 3 on 2026-04-21, `audit_events` added by Phase 5 on 2026-04-22; 2026-02 → 2026-04). Phase 4 PR 1 (2026-04-21) + Phase 4 PR 2 (2026-04-22) added 0 migrations — pure policy enforcement on the Phase 2 data model. Phase 5 added 1 migration (`20260420120007_audit_events.sql`).
- **Vector DB:** LanceDB 0.27 (embedded vector + FTS for memory)
- **Key-Value Store:** redb 4.1 (embedded; bumped from 4.0 in PR #101 deps roll-up)
- **LLM Framework:** Rig v0.35 (agentic loop framework). LiteLLM proxy is a first-class provider as of 2026-04-20 (PR #78; UI Update/Test/Delete modal completed in PR #80 with baseUrl, use_bearer_auth, and extra_headers fields; Azure singular [llm.provider.azure] → plural [llm.providers.azure] migration landed in the same PR with read-fallback for legacy configs): `[llm.providers.litellm]` block with `api_type = "openai_completions"` transport (no new `ApiType` variant), `litellm_api_key` field on `LlmConfig` (System-category, virtual API key — NOT the proxy's admin master key), `ProviderStatus.litellm` bool for UI detection, `litellm/`-prefixed model names skip Spacebot's rate-limit tracking because the LiteLLM Router owns those semantics.
- **CLI:** clap 4.5 (current Cargo.toml pin; earlier memory claim of 4.6 was drift).
- **Serialization:** serde/serde_json 1.0
- **Logging:** tracing 0.1
- **Error Handling:** thiserror + anyhow
- **Discord:** Serenity (rev-pinned from `next` branch at `1cbceb275b10566145b0bdca1c57da9502079a6a` — pulls rustls 0.23 + tokio-tungstenite 0.28 to resolve rustls-webpki audit advisories. Rev-pin replaced branch-tracking 2026-04-19 to stop `cargo update` re-fetches from invalidating incremental cache.)
- **WebSocket:** tokio-tungstenite 0.28
- **Metrics:** Prometheus (feature-gated behind `metrics` feature)
- **macOS Keychain:** security-framework 3
- **Hashing:** sha2 0.11 (Phase 5 audit chain), subtle 2 (Phase 0 constant-time compare), hex 0.4.
- **UUID:** uuid 1.15 with features `["v4", "v7", "serde"]` (v7 added in Phase 5 for `Uuid::now_v7()` in the audit appender).
- **Embeddings:** fastembed **exact-pinned to 5.13.2**. 5.13.3 pulled hf-hub 0.5.0 which broke ONNX model fetch across 8 memory tests. Relax the pin once hf-hub 0.5 compatibility is restored upstream.

## Frontend
- **Web UI:** Vite + React + TypeScript (`interface/`)
- **JS Package Manager:** bun (NEVER npm/pnpm/yarn)
- **Documentation Site:** Next.js + Fumadocs (`docs/`) — fumadocs-core + fumadocs-ui on 16.8.1, fumadocs-mdx on 14.3.1 as of PR #101
- **SpaceUI:** Design system (6 packages: tokens, primitives, forms, icons, ai, explorer) (`spaceui/`)
- **Internal packages (`@spacebot/*`):** `packages/api-client/` — TypeScript client for the Spacebot REST API + SSE event types. Activated 2026-04-19 (PR #75, commit `3cd80c5`). Consumed by `interface/` via `workspace:*` protocol. Codegen target for `just typegen` / `just check-typegen`. Subpath-only exports (`./client`, `./types`, `./schema`; no root barrel — see `openspec/specs/frontend-api-client/spec.md`). CI guard at `.github/workflows/ci.yml` (`check-typegen` job) validates schema freshness on every PR touching `src/api/**` or `packages/api-client/**`.
- **Spacedrive:** Spacebot-owned fork at `spacedrive/` — independent Cargo workspace with its own toolchain (`stable`), excluded via `[workspace] exclude = ["spacedrive"]` in root `Cargo.toml`. Per the 2026-04-16 self-reliance decision, `spacedrive/` is Spacebot engineering territory; `spacedriveapp/spacedrive` is a historical ancestor, not authority. Track A complete on main as of 2026-04-17: Phase 1 config (PR #54), Phase 2 HTTP client with `{"Query":...}` envelope + HTTPS enforcement + wiremock tests (PR #55), Phase 3 first agent tool `spacedrive_list_files` + prompt-injection envelope + pairing migration + secrets integration (PR #56). Runtime-gated behind `enabled`. Post-merge smoke test (Task 18) was blocked on the vendored fork not compiling; unblocked 2026-04-18 via PR #57 which stubbed 9 declared-but-unwritten sd-core modules + an `apps/web/dist/index.html` placeholder. Smoke test confirmed predicted Bearer/Basic 401 and surfaced a new envelope field-name mismatch (`input` vs `payload`). Both fixes are deferred to a dedicated session that will pair with a real-`sd-server` smoke rerun.
- **Desktop App:** Tauri (`desktop/`)

## Security

- Dependabot alerts in spacebot-owned code that are blocked on upstream crate updates are tracked in-repo at `docs/security/deferred-advisories.md` (GHSA, severity, blocker, unblock trigger)
- Non-dismissal policy: deferred advisories stay `open` on the Security dashboard; do NOT dismiss via the GitHub API
- Spacedrive-scoped Dependabot alerts (under `spacedrive/**`) remain open until the planned runtime integration; re-triaged at integration time
- `.github/dependabot.yml` covers all shipped-code manifests (root cargo, desktop/src-tauri, interface, packages/api-client, spaceui and its sub-manifests, docs); it controls update-PR scoping only, not security-alert visibility
- **Entra ID auth Phase 0 landed 2026-04-20 (PR #81, commit `2a40131`)**: constant-time bearer compare via `subtle = "2"` crate (closes obs #25356); JWT-shape regex added to `LEAK_PATTERNS` at `src/secrets/scrub.rs`; `spacebot_auth_failures_total{branch, reason}` Prometheus counter backed by a typed `AuthRejectReason` enum with 4 reasons (`header_missing`, `header_non_ascii`, `scheme_missing`, `token_mismatch`) and an exhaustiveness-guarded label-snapshot test that compile-fails if a variant is added without a corresponding assertion; CORS-credentials regression test (with positive `access-control-allow-origin` assertion proving the CorsLayer is engaged); `ApiState::new_for_tests` + `api::test_support::build_test_router` always-compiled `#[doc(hidden)]` test scaffolding. Static `auth_token` path preserved unchanged.
- **Phase 1 landed 2026-04-20 (PR #82, squash commit `91b8e38`)**: new `src/auth/` module tree (`errors`, `context`, `config`, `jwks`, `middleware`) validates Entra v2 JWTs via `jwt-authorizer 0.15` with RS256 pinned from `jsonwebtoken 9`, distinguishes User (delegated, has `scp`) vs ServicePrincipal (app-only, no `scp`), enforces required-scope for users plus required-role for SPs, and emits the `entra_jwt` metric branch. `ApiState.entra_auth: Arc<ArcSwap<Option<Arc<EntraValidator>>>>` follows the `task_store`/`wiki_store` setter pattern. Branch selection in `start_http_server` picks Entra when configured, static-token otherwise (never both attached). Both branches now populate `AuthContext` into request extensions. Precursor: existing `src/auth.rs` (Anthropic OAuth PKCE) renamed to `src/anthropic_oauth.rs` to free the `auth` namespace (10 call sites migrated, git-preserved). Test coverage: 5 unit (`auth::context`, `auth::config`) + 16 integration (`tests/entra_jwt_middleware.rs`: 11 validator-level + 5 router-level via `build_test_router_entra`) + Wiremock-backed `tests/support/mock_entra.rs` with pure-Rust `rsa 0.9.10` keypair generation. Config surface: `[api.auth.entra]` TOML block with `env:`/`secret:` resolution via new `resolve_required_ref` helper that fails-fast on unresolvable references. Design record: `docs/design-docs/entra-app-registrations.md`.
- **Phase 2 landed 2026-04-21 (PR #101, squash commit `bd7579b`)**: authz data model + fire-and-forget user upsert. Five additive instance-level migrations (`users`, `teams`, `team_memberships`, `service_accounts`, `resource_ownership` — the last is a sidecar keyed on `(resource_type, resource_id)` per research §12 Alternative-1 rather than sprouting owner/visibility columns on 20 per-agent tables). Identity key is composite `{tid}:{oid}` surfaced as `principal_key`. CHECK constraints lock every authz enum column (`visibility`, `principal_type`, `users.status`, `teams.status`, `source`) to its allowed value set. New `src/auth/principals.rs` (record types + `Visibility` enum) and `src/auth/repository.rs` (CRUD helpers) target the instance pool. `upsert_user_from_auth` returns a typed `RepositoryError` enum (`InvalidPrincipalType` for legacy-static rejection, `Sqlx(#[from] sqlx::Error)` otherwise); the other three helpers return `anyhow::Result` with `.with_context()`. `ApiState.instance_pool: ArcSwap<Option<sqlx::SqlitePool>>` mirrors the setter pattern. Middleware fires-and-forgets the upsert on successful Entra auth, wrapped in `tracing::info_span!("auth.upsert_user", principal_key=...).instrument(...)`. Two new Prometheus counters: `spacebot_auth_upsert_failures_total{reason}` (reason ∈ `invalid_principal_type | sqlx`) and `spacebot_auth_upsert_skipped_total` (fires only when `instance_pool` is unattached, a persistent non-zero is a startup-ordering regression). Test coverage: 12 integration tests in `tests/authz_data_model.rs` (all 5 migrations apply cleanly, user + team upsert idempotency, ownership round-trip, LegacyStatic rejection contract, None-for-missing, plus 5 raw-SQL tests — one per CHECK constraint). Design record: `docs/design-docs/entra-backfill-strategy.md` documents the no-auto-broadening policy for pre-existing resources and points at the Phase 10 orphan-sweep design. PR #101 also folded 11 safe Dependabot updates (clap 4.6.1, uuid 1.23.1, open 5.3.4, redb 4.1, tokio 1.52.1, tracing-appender 0.2.5, rmcp 1.5, desktop tracing-subscriber 0.3.23, docs fumadocs 16.8.1 + fumadocs-mdx 14.3.1). NOTE: clap was subsequently rolled back to 4.5 at some point between PR #101 and Phase 5; current Cargo.toml reads `clap = { version = "4.5" }`.
- **Phase 3 landed 2026-04-21 (PR #103, squash commit `60b17c3`)**: Microsoft Graph API client + group resolution + A-19 photo persistence. New `src/auth/graph.rs` (222 lines) with OBO token exchange, `list_member_groups` via `/me/getMemberObjects`, and `fetch_user_photo` via `/me/photo/$value` (single `User.Read` OBO scope covers both). New migration `20260420120006_users_photo.sql` adds nullable `display_photo_b64` + `photo_updated_at` columns. Middleware fires-and-forgets group sync (transactional DELETE+INSERT replace-set on `team_memberships` with `group_cache_ttl_secs` freshness check, 300s default) and photo sync (weekly TTL) after successful auth. 202 Accepted first-request-per-principal race handler (A-10) for the empty-`team_memberships` window. `ApiState.graph_client: ArcSwap<Option<Arc<GraphClient>>>` + `set_graph_client` setter. Two new Prometheus counters: `auth_first_request_race_total` and `auth_graph_sync_failures_total{op, reason}`. `GraphConfig.web_api_client_secret` redacted via manual `Debug` impl. 25 integration tests across `entra_jwt_middleware.rs` (+1 A-10 router test) and new `graph_integration.rs` (8 tests: overage resolution, fail-closed, cache-TTL skip, photo persist, absent-photo, stale-cache refetch, token-claim path, OBO 400 failure). Design record: `docs/design-docs/entra-app-registrations.md` (rewritten Graph API permissions section for delegated `User.Read`).
- **Phase 4 PR 1 landed 2026-04-22 (PR #104, squash commit `4716e38`)**: authz helpers + AuthContext plumbing + memories handler proof-of-pattern. New `src/auth/roles.rs` (ROLE_ADMIN/USER/SERVICE constants + `require_role`/`is_admin` helpers). New `src/auth/policy.rs` — pure policy module with `Access` (`Allowed | Denied(DenyReason)`, `#[must_use]`), `check_read`/`check_write`/`check_read_with_audit` (latter returns `(Access, admin_override)` where `admin_override` is narrowed to `ctx.has_role(ROLE_ADMIN) && !is_owner` so Legacy/System bypass without auditing), `can_link_channel` (`#[must_use]`, enforcement deferred to PR 2). New `src/auth/testing.rs` (plain `pub mod` with `#[doc(hidden)]`) — `MockValidator` + `mint_mock_token` for integration tests, `TryFrom<MintableAuthContext>` rejects unknown principal_type strings instead of silently downgrading to LegacyStatic. `src/auth/jwks.rs` gains `JwtValidator` RPITIT trait + `DynJwtValidator` companion via blanket-impl `Box::pin`; `ApiState.entra_auth` migrated from concrete `Arc<EntraValidator>` to `Arc<dyn DynJwtValidator>`. New companion field `ApiState.entra_config: Arc<ArcSwap<Option<EntraAuthConfig>>>` plus one-shot `AtomicBool` warn guard `entra_config_missing_warned`. `AgentDeps.auth_context: AuthContext` (new field at `src/lib.rs:446`) carries originating principal through Channel/Branch/Worker spawn via `#[derive(Clone)]`; `AgentDeps::for_turn(ctx)` builder (`#[must_use]`).
- **Phase 4 PR 2 landed 2026-04-22 (PR #105, squash commit `bb9156c`)**: 10-handler rollout + cross-agent link enforcement + TOML reconciliation. Rolls the PR-1 proof-of-pattern across 10 resource families (memories/tasks/wiki/cron/portal/agents/notifications/projects/attachments/ingest) with byte-uniform inline gate blocks per N1. Cross-agent enforcement wires `can_link_channel` into `SendAgentMessageTool::call()` at the sole ingress. Channel turn-dispatch threads per-request principal via new `InboundMessage.auth_context: Option<AuthContext>` (#[serde(default)]) + `Channel::install_turn_deps` mutating self.deps + self.state.deps. TOML reconciliation at startup after migrations + before HTTP binds. 55+ integration tests across 10 `tests/api_<resource>_authz.rs` files. Two review-cycle passes (C1 Pool-None fail-open escalation, D1-D3 byte-drift, W1 em-dash sweep, NEW-1/2/3 batch) all closed.
- **Phase 6 PR A landed 2026-04-23 (PR #107, squash commit `f15d11d`)**: SPA integration — MSAL.js v5 + unprotected `/api/auth/config` bootstrap + AuthGate state machine + operator docs. First of three Phase 6 PRs (B: `authedFetch` + 88-call-site migration; C: `EventSource` → `@microsoft/fetch-event-source` SSE polyfill). Backend: new `src/api/auth_config.rs` handler + `PublicEntraConfig` projection at `src/auth/config.rs` + new `src/auth/bypass.rs` module extracting the shared `/api/health` / `/api/auth/config` path-allowlist consumed by both middleware branches (matches `/api/health` precedent, prevents drift between static-token + Entra-JWT allowlists). `ApiState::entra_auth_public_config()` reads the companion `entra_config: Arc<ArcSwap<Option<EntraAuthConfig>>>` via the canonical `.load().as_ref().as_ref().map()` chain. Frontend: new `interface/src/auth/{AuthGate.tsx,msalConfig.ts}` with 5-state gate (loading/entra_disabled/unauthenticated/authenticated/error), A-16 `memoryStorage` default + A-17 `localStorage` opt-in via "Stay signed in on this device" checkbox, operator-visible error banner when `/api/auth/config` returns malformed config. MSAL v5 API drift caught at impl time: `storeAuthStateInCookie` removed from `CacheOptions`, `allowNativeBroker` → `allowPlatformBroker` renamed (F17). Redirect URI set to `/` (not `/auth/callback` — no router route exists; MSAL processes the fragment above RouterProvider) per F18. `setAuthTokenProvider` + `getAuthToken` primitives added to `packages/api-client/src/client.ts` so AuthGate has a real consumer; PR B will layer `authedFetch` around them. Test infrastructure: vitest + @testing-library/react first landing in `interface/` (zero `*.test.tsx` files existed before PR A). Test counts: 3 new Rust integration tests in `tests/api_auth_config.rs` (unprotected, no leaks against a populated `EntraAuthConfig`, populated-path shape assertion), +1 regression test in `tests/api_auth_middleware.rs` (static-token bypass), +1 in `tests/entra_jwt_middleware.rs` (Entra-JWT bypass), +3 `auth::bypass::tests` unit tests (sort/uniqueness invariant, documented-entries match, exact-match only), +4 vitest tests in `AuthGate.test.tsx` (loading/entra_disabled/malformed/fetch-failure), +5 vitest tests in `authTokenProvider.test.ts` (unset/yield/null/throws/re-set). Review cycle remediations applied in-branch: C1 (leak-scrub was vacuously-true; now runs against populated fixture via `new_test_state_with_mock_entra_configured`), C2 (populated-path coverage), C3 (Entra-JWT allowlist regression), I3 (shared `auth::bypass` module), I4/I5/S7 (fail-loud on malformed config via new `MsalInstanceResult` discriminated union), I6 (primitives tested), F19 (CI runner disk-space — new "Free disk space" step in Test job reclaims ~25 GB of unused preinstalled SDKs). Operator guide: `docs/content/docs/(configuration)/entra-auth.mdx` (198 lines, 4 troubleshooting scenarios, cross-links to entra-app-registrations / entra-backfill-strategy / entra-audit-log / entra-role-permission-matrix). No migrations — 55 total unchanged. PR A deferred to PR B: authedFetch + 88 fetch-site migration; deferred to PR C: SSE polyfill + `useEventSource` rewrite + `/api/me` consolidation per A-18.
- **Phase 6 PR B landed 2026-04-23 (PR #108, squash commit `524d366`)**: `authedFetch` sibling module + 88-call-site migration + token-provider wiring. Second of three Phase 6 PRs. New `packages/api-client/src/authedFetch.ts` (173 lines) — drop-in `fetch()` replacement routing through PR A's `getAuthToken()` primitive, attaching `Authorization: Bearer <token>` when a provider is set. Two-budget retry state machine (AUTH_CAP=1 for 401-refresh, SYNC_CAP=3 for 202 Graph-sync race per A-10, TOTAL_CAP=5 safety net). Preserves FormData + Blob browser-managed Content-Type by deleting any caller-supplied header so multipart boundaries + MIME types are browser-computed. D3 null-provider-on-retry guard (dispatches `spacebot:auth-exhausted` CustomEvent with reason `no_token_on_retry`). D9 Retry-After parser: 1000ms fallback on absent/malformed/negative, 10s clamp against malicious-proxy sleep. O2 observability: `spacebot:auth-exhausted` also fires on `refresh_failed` path. O3 privatization: `authedFetchInner` + `RetryState` module-private; public `authedFetch(input, init?)` has no budget-tuning knobs. O6 retry-token threading: 401 retry passes `preResolvedToken` into recursive call so the token provider isn't queried twice per attempt. New exports: `authedFetch` (via `./authedFetch` entry in package.json + re-exported from `./client`), `AuthExhaustedReason`, `AuthExhaustedDetail`. 88 inline `fetch(` call sites in `packages/api-client/src/client.ts` sed-migrated to `authedFetch(` (1× `fetchJson` helper + 87× inline api-object closures ~lines 1400-2600). SSE intentionally untouched — PR C swaps `EventSource` for `@microsoft/fetch-event-source` per G6 deploy-ordering (do NOT enable `[api.auth.entra].enabled=true` between PR B and PR C merges or SSE returns 401). Structural win per O1: `authedFetch` in a sibling module eliminated the sed-recursion class entirely; `client.ts` contains zero `fetch(` calls post-migration. `fetchJson` error message pattern upgraded to `API error <status>: <path>` for Sentry breadcrumb identifiability (D17 pattern downstream). Test coverage: 19 vitest cases in `interface/src/auth/__tests__/authedFetch.test.ts` covering header attachment, FormData + Blob Content-Type safety (including the G4 explicit-delete-on-collision test), 401-retry + D3 null-provider case, disabled-mode 401 passthrough, deterministic D8 `401→202→401 = 3 calls` trace, sync-cap fires at attempt 4, auth-exhausted CustomEvent dispatch + detail.reason assertion for both paths, and direct `parseRetryAfterMs` tests (null/NaN/negative/10s clamp/boundary). interface suite total: 15 → 28 tests. Review cycle: 5-agent parallel review (code-reviewer, pr-test-analyzer, silent-failure-hunter, comment-analyzer, type-design-analyzer) surfaced 0 Critical + 4 Important + 6 Suggestion findings; all 10 folded into 3 remediation commits (`72ae4af` em-dashes + stale sed-guard comment rewrite, `e060d06` AuthExhausted type extraction + `totalAttempts` derivable-state removal + retry-token threading + SSR guard helper + fetchJson path, `988bc1d` auth-exhausted event coverage tests + Content-Type-delete-on-collision test + parseRetryAfterMs direct tests). No Rust changes — 0 new migrations, 0 new integration tests. PR C scope: `EventSource` → `fetchEventSource` swap (transport-only per D13/D14 correction; existing `handlers: Record<string, EventHandler>` + `enabled` + `onReconnect` + `{ connectionState }` return contract preserved so `useLiveContext`, `Overlay`, and `ConnectionBanner` work unchanged) + `/api/me` consolidated endpoint + `mockMsal` for VITE_AUTH_MOCK + user menu with sign-out + global `spacebot:auth-exhausted` listener in AuthGate + docs delta appending to PR A's existing `entra-auth.mdx` (NOT overwriting).
- **Phase 6 PR C landed 2026-04-23 (PR #109, squash commit `7bd6b43`)**: SSE polyfill + `/api/me` + user menu + mock mode + operator docs delta. Third and final Phase 6 PR. Backend: new `src/api/me.rs` (~175 lines) exposing `GET /api/me` returning `MeResponse { principal_key, tid, oid, principal_type: PrincipalType, display_name, display_email, display_photo_data_url, initials, roles, groups, groups_overage }`. `PrincipalType` now derives `utoipa::ToSchema` so the generated schema emits a proper string-literal TS union `"user" | "system" | "service_principal" | "legacy_static"` rather than an opaque `String` — `principal_type` consumers get exhaustive narrowing at compile time. New total helper `compute_initials(name: &str) -> String` (whitespace-only + empty-string inputs return "?", not literal space). DB sqlx errors surface `tracing::warn!(instance_pool=None)` so operators have a grep target on the 202 race window. No new migration — the endpoint reads `users` + `users_photo` (Phase 2) + `team_memberships` (Phase 2) tables. Frontend: new `interface/src/auth/useMe.ts` exporting `useMe()`, `useRole(role: RoleLike)`, `useMyPrincipalKey()` with `RoleLike = "SpacebotAdmin" | "SpacebotUser" | "SpacebotService" | (string & {})` for IDE autocomplete without closing the set. 5-minute staleTime matches daemon `group_cache_ttl_secs`. Error-message pattern `API error <status>: <path>` matches PR B's `fetchJson`; malformed-JSON path surfaces as `API error: malformed JSON from <path>` (distinguishable from network failure). New `interface/src/components/UserMenu.tsx` uses MSAL `logoutRedirect({ account, postLogoutRedirectUri: window.location.origin })`; returns null gracefully when `accounts[0]` absent (entra_disabled mode). Mounted in `App.tsx` AppShell header. New `interface/src/auth/mockMsal.ts` powers `VITE_AUTH_MOCK=1` — mints base64url JSON tokens compatible with the daemon's `MockValidator`; exports `base64UrlEncode(bytes: Uint8Array): string` helper for direct testing; `setActiveAccount(a: AccountInfo | null)` (not `unknown`); `logoutRedirect(opts?: { postLogoutRedirectUri? })` honors subpath deployments. `interface/src/hooks/useEventSource.ts` transport swapped from native `EventSource` to `@microsoft/fetch-event-source`; hook contract (`handlers: Record<string, EventHandler>` + `enabled?` + `onReconnect?` + return `{ connectionState }` + `ConnectionState` type) preserved byte-for-byte so `useLiveContext`, `Overlay`, `ConnectionBanner` required zero caller changes. Exponential backoff via `onerror` delay-return: `[1000, 2000, 4000, 8000, 16000, 30000, 30000]` clamped at `MAX_RETRY_MS=30_000`. `spacebot:auth-exhausted` global listener wired in `interface/src/auth/AuthGate.tsx` (lines 48-58) covers both REST (authedFetch 401-refresh-exhausted) and SSE (via `fetchEventSource(fetch: authedFetch)`); currently logs to `console.warn`, Phase 7 PR 5 replaces the handler body with toast + Re-sign-in CTA (do NOT add a second listener). `authedFetch` sibling-module exports now also available at `@spacebot/api-client/authedFetch` (direct path) in addition to the re-export from `./client`. `AuthExhaustedReason` + `AuthExhaustedDetail` types re-exported from package root for downstream listener typing. Operator guide: `docs/content/docs/(configuration)/entra-auth.mdx` grew 198 → 296 lines with 5 new sections ("Server-sent events under Entra", "Sign-out and the user menu", "`/api/me` consolidated principal endpoint (A-18, A-19)", "Local development — VITE_AUTH_MOCK", "Additional troubleshooting (PR C surfaces)"). Unified 7-day JWKS TTL vocabulary across code + docs (replacing "weekly" in `src/auth/middleware.rs:538` operator doc line 86). Test coverage: interface vitest grew 28 → 47 tests across 6 files (authedFetch 19, authTokenProvider 5, AuthGate 6, mockMsal 6, useEventSource 6, useMe 5). Rust integration grew 40 → 41 files (new `tests/api_me.rs` with 6 cases including `service_principal_serializes_as_snake_case` and `photo_row_present_with_null_blob_falls_back_to_initials`). Rust unit coverage added 7 `compute_initials` cases to `src/api/me.rs::tests`. Schema regen added ~62 lines to `packages/api-client/src/schema.d.ts` for the `MeResponse` + `PrincipalType` union. Review cycle: 5-agent parallel review surfaced 2 Critical + 5 Important + 3 Suggestion findings; all 10 folded into 4 remediation commits (`031c19e` api/me refactor — C1 DB-error logging + I2 PrincipalType enum + I3 TTL vocab + I4 em-dashes + S1 compute_initials total + S3 whitespace→? + S7 strip-plan-anchors plus 2 integration tests; `e009467` spa refactor — I1 mockMsal typing + S2 useRole autocomplete + S6 useMe JSON-parse + S7 strip-plan-anchors; `1acd054` test expansion — C2 + I5: mockMsal.test.ts 6 cases + useEventSource +4 + AuthGate +2; `8691b9a` entra-auth.mdx 7-day TTL completion). Plus CI-only fix `4946334` widening `capturedSignal: AbortSignal | null | undefined` to satisfy `@microsoft/fetch-event-source`'s strict type under CI's `--frozen-lockfile`. Amendments applied in Phase 6: A-16 (memoryStorage default), A-17 (localStorage opt-in), A-18 (`/api/me` as single source of truth for roles + principal metadata, NOT `idTokenClaims.roles`), A-19 (sidebar header shows user avatar or initials from `/api/me`). Deferred from Phase 6 and candidates for later PR: Conditional Access claims-challenge (`WWW-Authenticate: Bearer claims=...` → `msal.acquireTokenRedirect({ claims })`), Playwright sign-in e2e (O5), cross-tab sign-in sync (A-17 memoryStorage vs localStorage UX), CSP header enforcement (Phase 10 SOC 2 scope).
- **Phase 7 PR 1 landed 2026-04-23 (PR #110, squash commit `e1cd5b2`)**: SPA shared primitives + auth-layer batch helpers. First of 5 Phase 7 PRs (per the plan's stated shipping boundary at line 41), scoped to reusable substrate — the actual resource-type rollouts live in PRs 2-5. Frontend: 4 new components + 1 helper added to existing module. (a) `interface/src/components/VisibilityChip.tsx` — labeled pill with `data-tone` attribute (neutral/info/success/warning); props are `visibility: Visibility` (closed union `"personal" | "team" | "org"`) + optional `teamName?: string` for team-scoped expansion. Runtime fallback renders "Unknown" for values that bypass TS narrowing (defense-in-depth against untyped wire data). 5 vitest cases including the ignores-teamName-when-personal contract pin. (b) `interface/src/components/VisibilityFilter.tsx` — controlled radiogroup with `role="radiogroup"` + `aria-label`. `VisibilityFilterValue = "all" | Visibility` is a DERIVED union (not hand-duplicated literals) so future Visibility enum changes propagate here automatically via typegen. 2 vitest cases. (c) `interface/src/components/ShareResourceModal.tsx` — dialog for changing visibility + team binding. Key contract: `onSubmit: (args: ShareSubmitArgs) => Promise<void>` where `ShareSubmitArgs` is a DISCRIMINATED UNION `{ visibility: "team"; sharedWithTeamId: string } | { visibility: "personal" | "org" }` — `{personal + teamId}` illegal state is unrepresentable at the type level. Error handling narrows to API errors (`Error.message.startsWith("API error")`) and rethrows programmer errors via the React error boundary; API errors log via `console.error` before showing the human-readable message in a `role="alert"`. `aria-describedby` wired to a screen-reader-only description using `resourceType` + `resourceId`. 4 vitest cases covering happy path + empty-team-guard + API-error rejection path + programmer-error rethrow. (d) `interface/src/auth/useMe.ts` gained `useMyPrincipalKey(): string | null` appended beneath the existing `useMe()` + `useRole()` (per D27 — do NOT create a separate `useRole.ts`). Returns `data?.principal_key ?? null` so pre-load consumers render a neutral placeholder rather than "undefined" or throwing. 2 vitest cases including the empty-string contract pin (server sends `""` → helper passes through, so `if (!key)` as a signed-in gate still fails closed). Backend: `src/auth/repository.rs` gained two batch helpers: `list_ownerships_by_ids(pool, resource_type, &[String]) -> HashMap<String, ResourceOwnershipRecord>` and `get_teams_by_ids(pool, &[String]) -> HashMap<String, TeamRecord>`. Both short-circuit on empty input (no pool roundtrip). Missing entries are ABSENT from the returned map so handlers can distinguish "no ownership row" (render as Legacy per the no-auto-broadening policy in `entra-backfill-strategy.md`) from "personal ownership row with visibility='personal'". Placeholder-binding pattern follows `MemoryStore::get_associations_between` (cited by function name for rot-proofing, not by line number). 5 new tokio tests in `tests/authz_data_model.rs` including dedupe-duplicate-inputs + N=500 safe-batch documenting test pinning the SQLITE_MAX_VARIABLE_NUMBER boundary. Architectural finding leading to these helpers (D36, pre-code audit 2026-04-23): the plan's original Task 7.5a proposed inline SQL LEFT JOIN in each of 4 list handlers; a parallel backend-architect + Explore audit confirmed SQLite does not support cross-DB JOIN, and 3 of 4 handlers (Memories, Cron, Agents) use per-agent pools or in-memory config. Only Tasks is in the instance pool where inline JOIN is architecturally feasible. The correct pattern for the other 3 is post-fetch enrichment via these batch helpers, landing in PR 1.5. Test coverage: interface vitest grew 47 → 61 tests across 10 files (+14: VisibilityChip 5, VisibilityFilter 2, ShareResourceModal 4, useMyPrincipalKey 3). Rust integration grew in `authz_data_model.rs` from 12 → 17 tests. No new migrations. No API endpoints added. No schema.d.ts regen needed. Review cycle: 5-agent parallel review surfaced 2 Critical + 4 Important + 9 Suggestion findings; 12 folded into 3 remediation commits (`45a0cc0` error narrowing + a11y + 2 new tests, `3a37e60` ShareSubmitArgs discriminated union refactor, `37b749d` polish — derived union + batch-dedupe + teamName-ignored + empty-string contract + store.rs by-function-name + em-dash fixes + N=500 documenting test), 3 explicitly deferred to PR 1.5 (S5 console.warn on unexpected visibility, S6 tid/oid correlation in context frames, S9 `TeamSummary` type in `@spacebot/api-client`). Deferred to PR 1.5 (BLOCKED-ON-PR-1.5 boundary): Task 7.5a list-handler DTO enrichment (add `visibility: Option<String>` + `team_name: Option<String>` to `MemoriesListResponse`, `TaskListResponse`, `CronListResponse`, `AgentsResponse`) + Task 7.5 `PUT /api/resources/{type}/{id}/visibility` endpoint in new `src/api/resources.rs` + `api.setResourceVisibility` wire helper in `packages/api-client/src/client.ts` + `just typegen` regen. PR 2 (Memories rollout) is blocked on PR 1.5 per the 7 D-corrections D37-D43 recorded inline in `.scratchpad/plans/entraid-auth/phase-7-ui-surfaces.md` header (gitignored).
- **Phase 5 landed 2026-04-22 (PR #106, squash commit `cda7da2`)**: hash-chained audit log + WORM export. SOC 2 CC7.2 evidence layer. 20 commits (14 feature + 4 remediation + 2 docs-audit carryover) across 42 files (+3421/-232). New `src/audit/` module tree (`types` with `AuditAction` enum + `canonical_bytes`, `appender` with A-13 singleton discipline + tokio-Mutex-serialized narrow transaction + `verify_chain`, `export` with 3 ExportMode variants per A-15). New migration `20260420120007_audit_events.sql` (hash-chained `audit_events` + `audit_export_state` incremental-cursor table per A-14). New `src/api/audit.rs` (admin `/api/admin/audit` NDJSON/CSV + `/api/admin/audit/verify` endpoints). `ApiState.audit: ArcSwap<Option<Arc<AuditAppender>>>` attached atomically with `instance_pool` in `set_instance_pool`. Middleware emits `AuthSuccess` / `AuthFailure` per request. `src/auth/policy.rs` gains `fire_admin_read_audit` + `fire_denied_audit` helpers; 25 Phase-4-era tracing stubs across 10 handler families converted. A-01 scrubbing via `scrub_leaks` BEFORE `canonical_bytes` hashes the row. New `[audit.export]` TOML block + top-level `tokio::spawn` scheduler in `main.rs` with consecutive-failure backoff. Task 5.7b (Phase 4 deferred item #1): `Channel::install_turn_deps` + new `restore_turn_deps` pair with `#[must_use] PriorTurnDeps` — `handle_message` / `handle_message_batch` refactored to thin outer wrappers + `_inner` bodies. Plan's `scopeguard::ScopeGuard<T, F+'_>` approach proved unimplementable (borrow-checker E0499); Option E-lite explicit install/restore landed instead. Test coverage: 14 new tests across `tests/audit_chain.rs` (5), `tests/audit_scrubbing.rs` (2), `tests/audit_export.rs` (3 including incremental cursor), `tests/api_admin_audit.rs` (6 total: non_admin_403, admin_ndjson, CSV Accept, verify clean, verify tampered, non_admin_verify_403), `tests/channel_turn_deps.rs` (3), `tests/audit_middleware_emission.rs` (2), `tests/audit_handler_emission.rs` (2). Plus 5 new unit tests in `src/api/audit.rs::tests` (csv_escape RFC-4180 + formula-injection guard, accept_prefers_csv first-token-wins). `grep "admin_read override (audit event queued for Phase 5)" src/api/` returns 0. Amendments applied: A-01, A-02, A-03, A-13, A-14, A-15. Operator guide: `docs/design-docs/entra-audit-log.md`. 6-agent parallel review flagged 8 Critical/Important + 9 minor findings; 4 remediation commits folded. Key plan corrections: Task 5.6 principal_type snake_case (centralized in `PrincipalType::as_canonical_str`), Task 5.7b scopeguard → explicit restore, Task 5.8 utoipa routes!-macro auto-discovery + `/admin/audit` nested path (not `/api/admin/audit`), Task 5.10 scheduler in main.rs (not cortex).

## Tooling (opt-in)

- **Graphify** — local knowledge-graph tool installed per-developer via `pipx install graphifyy`. Three opt-in just recipes (`graphify-rebuild`, `graphify-query`, `graphify-clean`) wrap a `--directed`-preserving shell script. Not wired into CI or hooks. `.graphifyignore` at repo root mirrors `.gitignore` text-content entries plus graphify-specific exclusions (spacedrive/, vendor/, packages/api-client/src/schema.d.ts, graphify-out/ self-exclusion). Complements the existing `code-review-graph` MCP (which handles structural Rust code queries); graphify's value is semantic cross-document clustering for design docs, RFCs, and screenshots. Landed 2026-04-21 alongside Phase 3 Entra work on `feat/entra-phase-3-graph-client`.

## Deployment
- Docker images published to GHCR (ghcr.io/jrmatherly/spacebot)
- Single binary, no server dependencies
- All data in embedded databases in a local data directory
- Release builds for Linux (x86_64, aarch64) and macOS (aarch64 only). Windows was dropped for `daemonize` Unix-only dep + bun-on-Windows preinstall gaps; Intel macOS dropped because `ort-sys` ships no prebuilt for `x86_64-apple-darwin`. Both exclusions are documented inline in `.github/workflows/release.yml`.
