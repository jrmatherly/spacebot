# Spacebot - Project Structure

Single binary crate with no workspace **members**. The root `Cargo.toml` carries `[workspace] exclude = ["spacedrive"]` to keep the vendored Spacedrive workspace out of Cargo auto-discovery.

```
spacebot/
├── src/                  # 236 Rust source files (Phase 6 PR A added 2: src/api/auth_config.rs and src/auth/bypass.rs; Phase 6 PR B added 0 Rust files — the PR was TypeScript-only, scoped to interface/ + packages/api-client/; Phase 6 PR C added 1: src/api/me.rs — 175-line `GET /api/me` handler exposing `MeResponse` + `compute_initials` total helper)
│   ├── main.rs           # CLI entry point (clap subcommands: start, stop, restart, status, skill, auth, secrets). Phase 5 (PR #106, 2026-04-22): top-level `tokio::spawn` audit-export scheduler around ~line 1692; MissedTickBehavior::Delay consume-first-tick pattern; consecutive-failure backoff (first 2 Err at `tracing::error!`, subsequent suppressed to `debug!` until a success resets). Reads `config.audit.export` populated at startup.
│   ├── lib.rs            # Library root — 36 public modules, shared types. InboundMessage.auth_context: Option<AuthContext> with #[serde(default)] added by Phase 4 PR 2. Phase 5 added `pub mod audit;` (alphabetical, between `api` and `auth`).
│   ├── bin/              # Extra binaries: openapi-spec, cargo-bump
│   ├── agent/            # Agent lifecycle & orchestration (channel, branch, worker, compactor, cortex). channel.rs gains Channel::turn_deps + Channel::install_turn_deps helpers in Phase 4 PR 2 — per-turn mutation of self.deps + self.state.deps before spawn; race-safe via &mut self serialization on the per-channel event loop. Phase 5 Task 5.7b (PR #106, 2026-04-22) closes the panic-window gap: new `PriorTurnDeps` struct with `#[must_use]` + new `Channel::restore_turn_deps` helper. `handle_message` and `handle_message_batch` refactored to thin outer wrappers calling `_inner` bodies (zero indentation churn on 400-line turn bodies). Restore runs on all Ok/Err return paths; panic path intentionally not restored (channel task dies on panic at bare tokio::spawn in main.rs). Async-cancellation path NOT restored either (documented explicitly on install_turn_deps). Plan's `scopeguard::ScopeGuard<T, F+'_>` approach proved unimplementable (borrow-checker E0499); explicit install/restore Option E-lite replaced it.
│   ├── api/              # Axum HTTP router & REST endpoints. Phase 6 PR A (PR #107, 2026-04-23) added `src/api/auth_config.rs` — unprotected `GET /api/auth/config` handler returning `AuthConfigResponse { entra_enabled, client_id, tenant_id, authority, scopes }` (all-None when entra_enabled=false; bypass is wired via the `auth::bypass::AUTH_BYPASS_PATHS` list, NOT a utoipa `security = ()` override). Handler uses bare `path = "/auth/config"` because `api_router()` nests under `/api` at assembly time. Phase 4 PR 2 gates 10 handler families: memories, tasks, wiki, cron, portal, agents, notifications, projects, attachments, ingest. Each handler file gets a module-level //! doc describing the N1 inline-at-each-call-site rationale + metric label cardinality + A-09/A-12 invariants. register_agent_ownership helper added to agents.rs. Phase 5 (PR #106, 2026-04-22): new `src/api/audit.rs` — admin-only `GET /api/admin/audit` (NDJSON default, CSV via `Accept: text/csv` with RFC 4180 escaping + OWASP formula-injection guard) and `GET /api/admin/audit/verify` (returns `VerifyChainResponse { valid, first_mismatch_seq, total_rows }`). Routes are nested under `/api` at `api_router()` assembly so `#[utoipa::path]` declares `/admin/audit` (not `/api/admin/audit`). utoipa auto-discovery via `utoipa_axum::routes!` macro, NOT a `components(schemas)` list. 25 Phase-4-era `tracing::info!("admin_read override (audit event queued for Phase 5)")` stubs across 10 handler files all converted to `fire_admin_read_audit` calls; every `Access::Denied` branch now fires `fire_denied_audit`. `ApiState.audit: ArcSwap<Option<Arc<AuditAppender>>>` attached atomically with `instance_pool` in `set_instance_pool` (A-13 singleton discipline — production construction ONLY at that call-site). Phase 6 PR C (PR #109, 2026-04-23): new `src/api/me.rs` (~175 lines) — `GET /api/me` returning `MeResponse { principal_key, tid, oid, principal_type: PrincipalType, display_name, display_email, display_photo_data_url, initials, roles, groups, groups_overage }`. `PrincipalType` now derives `utoipa::ToSchema` so the generated schema emits the string-literal TS union `"user" | "system" | "service_principal" | "legacy_static"` rather than `String`. New total helper `compute_initials(name: &str) -> String` (whitespace-only + empty-string inputs return "?"). DB sqlx errors emit `tracing::warn!(instance_pool=None)` with grep-able message. Bare utoipa path `/me` (nests under `/api` at assembly). Handler reads `users`, `users_photo` (Phase 2/3), and `team_memberships` (Phase 2) — no new migration. `pub mod me;` added to `src/api.rs`. Phase 7 PR 1.5 (PR #111, 2026-04-23): new `src/api/resources.rs` (~468 lines) — `PUT /api/resources/{resource_type}/{resource_id}/visibility` handler (owner/admin only, 400 guard before pool access, returns 404 on concurrent delete after check_write passed). Exposes `pub struct VisibilityTag { visibility: Option<String>, team_name: Option<String> }` with private fields + `VisibilityTag::new` constructor that drops team_name when visibility != "team" (S1 narrowing). Shared `enrich_visibility_tags(pool, resource_type, &[String]) -> HashMap<String, VisibilityTag>` helper batches ownership + team lookups + elevates sqlx failures to `tracing::error!`. Consumer list handlers in the same PR: `MemoriesListResponse.memories: Vec<MemoryListItem>`, `TaskListResponse.tasks: Vec<TaskListItem>`, `AgentsResponse.agents: Vec<AgentListItem>` (wrappers `#[serde(flatten)] VisibilityTag`), `CronJobWithStats` gains inline visibility + team_name fields. Each list handler's pool-None enrichment path emits `tracing::warn!(handler, count)` mirroring the authz-skipped pattern (I4). 13 tests: 8 integration in `tests/api_resource_visibility.rs` (owner happy, non-owner 404, admin bypass, team-without-team-id 400, invalid 400, rotation preserves agent_id, team rebind, unauthenticated 401), 8 unit in `src/api/resources.rs::tests` (4 enrich cases pinning D36 policy + 3 SetVisibilityRequest::validate cases + 1 tag-constructor narrowing), 2 unit in new `src/api/cron.rs::tests` (serde field-flat wire contract). Phase 7 PR 2 (PR #112, 2026-04-23, squash `c43e5d2`): `src/api/resources.rs` extended in-place (no new file) with `pub(super) struct TeamSummary { id: String, display_name: String }` (`#[non_exhaustive]` for additive evolution) and `list_teams_handler(State, AuthContext) -> Result<Json<Vec<TeamSummary>>, StatusCode>` gated by `require_role(&auth_ctx, ROLE_USER)` (403 for SpacebotService-only principals per security F2 remediation). Pool-None path elevated to `tracing::error!` with blast-radius message matching the `enrich_visibility_tags` severity convention. Critically: `VisibilityTag.visibility` type NARROWED from `Option<String>` to `Option<Visibility>` — `Visibility` enum gained `utoipa::ToSchema` so OpenAPI now emits a literal union `"personal" \| "team" \| "org"` and SPA consumers inherit exhaustive narrowing without any `as Visibility` casts. `VisibilityTag::new` enforces the same `team_name ⇒ Visibility::Team` invariant via enum pattern-match (was string comparison). `VisibilityTag::visibility()` accessor returns `Option<&'static str>` via `Visibility::as_str()` for backward compat with the flat cron DTO. The `enrich_visibility_tags` callsite parses the stored string via `ResourceOwnershipRecord::visibility_enum()` (already existed in Phase 2) — unrecognized strings collapse to None (defense-in-depth). `set_visibility`'s check_write warn log gained `is_admin = is_admin(&auth_ctx)` field (SU-4) so operators distinguish admin-path failures from user-path failures.
│   ├── audit/            # Phase 5 (PR #106, 2026-04-22): hash-chained audit log. `src/audit.rs` module root re-exports `AuditAppender`, `AuditAction`, `AuditEvent`, `AuditRow`. `types.rs` (144 lines): AuditAction enum (12 snake_case variants), AuditEvent pre-persist shape, AuditRow persisted shape (sqlx::FromRow + utoipa::ToSchema), `canonical_bytes` deterministic serializer with strict sorted-key JSON canonicalization. `appender.rs` (~230 lines): AuditAppender singleton with `pub(crate) fn new` (A-13) + `#[doc(hidden)] pub fn new_for_tests`, tokio::sync::Mutex-serialized narrow transaction (SELECT prior row + INSERT new + commit), `verify_chain()` holds the same mutex for auditor snapshot consistency. `export.rs` (~230 lines): three ExportMode variants per A-15 (Filesystem dev-only NOT tamper-evident, S3 + HttpSiem are `anyhow::bail!` stubs with Phase 10 wire-up references). `export_audit()` is A-14 incremental — reads `last_exported_seq` from `audit_export_state` keyed on mode.kind_str(), exports rows WHERE seq > cursor, UPSERTs cursor forward on success.
│   ├── config/           # TOML config: runtime, types, load, permissions, onboarding, watcher, providers. load.rs gains reconcile_toml_agents_with_ownership (startup backfill of [[agents]] blocks to synthetic legacy-static principal per §11.3) + ensure_legacy_static_user (FK-anchor row with principal_type='system' AND principal_key='legacy-static' composite-key sentinel) in Phase 4 PR 2. Phase 5 (PR #106, 2026-04-22): `types.rs` gains `AuditConfig { export: Option<AuditExportScheduledConfig> }` and `AuditExportScheduledConfig { config: ExportConfig, interval: Duration }`. `toml_schema.rs` gains `TomlAuditConfig` + `TomlAuditExportConfig` (`enabled`, `mode` string "filesystem"|"s3"|"http_siem", `dir`, `interval_secs` default 86400). `load.rs` resolves `[audit.export]` TOML; Phase 5 accepts only `mode = "filesystem"`; `s3` and `http_siem` reject at load time with `anyhow::bail!` referencing Phase 10 wire-up.
│   ├── conversation/     # Conversation state management
│   ├── factory/          # Agent creation from presets, identity management
│   ├── hooks/            # Event hooks system
│   ├── identity/         # Agent identity file management
│   ├── llm/              # LLM manager, model routing, pricing, Anthropic provider (auth, cache, tools, params)
│   ├── memory/           # Vector memory: LanceDB store, embeddings, search, maintenance, working memory
│   ├── messaging/        # Inter-process message bus. Every InboundMessage construction site (Discord/Email/Mattermost/Slack/Telegram/Twitch/Webhook/Signal/Portal adapters) updated by Phase 4 PR 2 to set auth_context: None — adapters are non-authenticated paths; dispatch-layer fallback resolves to AuthContext::legacy_static().
│   ├── opencode/         # OpenCode protocol, SSE streaming
│   ├── projects/         # Project management, git integration
│   ├── sandbox/          # Tool execution sandboxing
│   ├── secrets/          # Keystore (macOS Keychain), secret scrubbing. Phase 5 uses `scrub::scrub_leaks` (A-01) BEFORE `canonical_bytes` hashes the row, so JWT shapes and PEM blocks never survive into the chain.
│   ├── skills/           # Skill installation & registry
│   ├── spacedrive/       # Spacedrive integration (Track A complete): config, HTTP client with `{"Query":...}` envelope + HTTPS enforcement, error taxonomy, wire types including SdPath, prompt-injection envelope, build_client_from_secrets helper. Runtime-gated via `enabled` flag
│   ├── auth/             # Entra ID auth. Phase 6 PR A (PR #107, 2026-04-23) added `src/auth/bypass.rs` — shared `AUTH_BYPASS_PATHS: &[&str]` + `is_auth_bypassed(path)` helper consumed by BOTH middleware branches (`api_auth_middleware` in `src/api/server.rs` and `entra_auth_middleware` in `src/auth/middleware.rs`). Three unit tests enforce invariants: sort/uniqueness, documented-entries match, exact-match-only (prefix matching deliberately NOT supported). Same PR added `PublicEntraConfig` + `EntraAuthConfig::public()` projection at `src/auth/config.rs` (Serialize + ToSchema, plain `String` fields not `Arc<str>`) and the `auth_config` pub mod declaration at `src/api.rs`. Phase 1 (PR #82, 2026-04-20): errors, context, config, jwks, middleware. Phase 2 (PR #101, 2026-04-21): principals (record types + Visibility enum), repository (CRUD helpers + RepositoryError enum). Phase 3 (PR #103, 2026-04-21): graph (Microsoft Graph client, OBO, group resolution, photo fetch). Phase 4 PR 1 (PR #104, 2026-04-22): roles (ROLE_* constants + require_role/is_admin), policy (Access/DenyReason + check_read/check_write/check_read_with_audit/can_link_channel), testing (MockValidator + mint_mock_token, plain pub mod with #[doc(hidden)]). Phase 4 PR 2 (PR #105, landed 2026-04-22, squash commit `bb9156c`): AuthContext + PrincipalType now derive serde::Deserialize so InboundMessage round-trips cleanly through the wire format. Wraps jwt-authorizer 0.15 with RS256 pinned; jwks.rs carries `JwtValidator` RPITIT trait + `DynJwtValidator` dyn-compatible companion so `ApiState.entra_auth` holds `Arc<dyn DynJwtValidator>`. 11 files total. Siblings `src/auth.rs` (module root) and `src/anthropic_oauth.rs` (formerly `src/auth.rs`, renamed to free the namespace). Phase 5 (PR #106, 2026-04-22): `context.rs` gains `PrincipalType::as_canonical_str(self) -> &'static str` returning canonical snake_case ("user" | "service_principal" | "system" | "legacy_static"); used by the middleware AuthSuccess/AuthFailure emission AND by policy.rs `fire_admin_read_audit` / `fire_denied_audit` helpers (so ServicePrincipal admins are correctly attributed, not hardcoded "user"). `middleware.rs` emits AuthSuccess on every successful auth (carries source_ip from X-Forwarded-For / X-Real-IP) and AuthFailure on every rejection (principal_key = "unknown", result = AuthError::metric_reason() classifier). `policy.rs` gains `fire_admin_read_audit` (spawns AuditAction::AdminRead with metadata `{"reason": "break_glass"}`) and `fire_denied_audit` (spawns AuditAction::AuthzDenied), both using `tracing::warn!` on append failure (rust-essentials-compliant: no `let _ =` on Result). Phase 7 PR 1 (PR #110, 2026-04-23, squash `e1cd5b2`): `repository.rs` gained two batch-reader helpers following the placeholder-binding pattern proven in `MemoryStore::get_associations_between` (src/memory/store.rs): `list_ownerships_by_ids(pool, resource_type, &[String]) -> HashMap<String, ResourceOwnershipRecord>` for enrichment queries keyed on resource id, and `get_teams_by_ids(pool, &[String]) -> HashMap<String, TeamRecord>` for display-name lookups by team id. Both short-circuit on empty input. Missing entries are ABSENT from the returned map (not None-valued) so handlers can distinguish "no ownership row" from "personal visibility row" per the no-auto-broadening policy in `docs/design-docs/entra-backfill-strategy.md`. 5 new integration tests in `tests/authz_data_model.rs` (12 → 17): empty-short-circuit, happy-path map-keying with partial-hit semantics, team display-name resolution, dedupe-duplicate-inputs, N=500 safe-batch documenting test for the SQLITE_MAX_VARIABLE_NUMBER boundary. Phase 7 PR 1.5 (PR #111, 2026-04-23, squash `901be41`): `repository.rs` gained `update_visibility_only(pool, resource_type, resource_id, visibility, shared_with_team_id) -> anyhow::Result<bool>` helper (UPDATE-only, preserves `owner_agent_id` + `owner_principal_key`, returns false when zero rows affected so handlers can return 404 rather than silently creating ownership rows under the caller's principal). Per C1 remediation from PR #111 review — the original set_ownership-based implementation would have NULLed the agent link on every rotation. Regression-tested in `tests/api_resource_visibility.rs::rotation_preserves_owner_agent_id`. Phase 7 PR 2 (PR #112, 2026-04-23, squash `c43e5d2`): `repository.rs` gained `list_teams(pool) -> anyhow::Result<Vec<TeamRecord>>` helper backing the `GET /api/teams` endpoint. Query uses EXPLICIT column list (not `SELECT *` — SU-1 SQL-layer projection invariant) with `WHERE status = 'active' ORDER BY display_name, id`; the `, id` tiebreaker (SU-2) guarantees deterministic ordering for same-display-name teams across restarts. `principals.rs` — `Visibility` enum gained `utoipa::ToSchema` derive so the wire literal-union surfaces in schema.d.ts (key structural win of PR 2 remediation).
│   ├── tasks/            # Task CRUD & migration. store.rs gains `update_prefetched(current: Task, input: UpdateTaskInput) -> Result<Task>` in Phase 4 PR 2 — eliminates duplicate SELECT on 4 authz-gated write handlers that pre-fetch for the gate; existing `update(task_number, ...)` delegates to prefetched after its own get_by_number.
│   ├── telemetry/        # Prometheus metrics (feature-gated; Phase 2 added auth_upsert_failures_total{reason} and auth_upsert_skipped_total; Phase 3 added auth_first_request_race_total and auth_graph_sync_failures_total{op, reason}; Phase 4 PR 1 added authz_skipped_total{handler})
│   ├── tools/            # 49 LLM-callable tool files (64 tool implementations; `spacedrive_list_files` added 2026-04-17). Phase 4 PR 2: SendAgentMessageTool gains instance_pool + auth_context fields; call() consults can_link_channel before find_link_between (closes PR-1 TODO at line 180). factory_create_agent.rs calls register_agent_ownership after create_agent_internal to prevent ownership orphans.
│   └── wiki/             # Wiki pages CRUD & search
├── interface/            # Web UI (Vite + React + TypeScript). No local src/api/ anymore — consumes `@spacebot/api-client` from packages/ via workspace symlink (PR #75, 2026-04-19). Vitest infrastructure landed in Phase 6 PR A (PR #107, 2026-04-23) with 9 initial tests across `authTokenProvider.test.ts` (5) + `AuthGate.test.tsx` (4). Phase 6 PR B (PR #108, 2026-04-23) added 19 `authedFetch.test.ts` cases; interface suite totaled 28 tests post-PR-B. Phase 6 PR C (PR #109, 2026-04-23) added 19 cases across 3 new files + 2 expansions: `mockMsal.test.ts` (6 cases — URL-safe base64 alphabet contract), `useEventSource.test.tsx` (6 cases — backoff sequence, lagged→onReconnect, unmount abort, enabled:false), `useMe.test.tsx` (5 cases — happy path, malformed JSON, network failure, 401, re-render same data), plus AuthGate.test.tsx +2 cases (auth-exhausted listener dispatch + unmount cleanup, growing 4 → 6). Interface suite now totals 47 tests across 6 files (authedFetch 19 + authTokenProvider 5 + AuthGate 6 + mockMsal 6 + useEventSource 6 + useMe 5). PR C non-test surfaces: `interface/src/auth/useMe.ts` (exports `useMe()` + `useRole(role: RoleLike)` + `useMyPrincipalKey()` with 5-min staleTime matching daemon `group_cache_ttl_secs`; `RoleLike = "SpacebotAdmin" | "SpacebotUser" | "SpacebotService" | (string & {})`); `interface/src/auth/mockMsal.ts` (VITE_AUTH_MOCK=1 path — mints base64url JSON tokens compatible with daemon `MockValidator`, exports `base64UrlEncode` helper, `setActiveAccount(AccountInfo | null)`, `logoutRedirect` honors subpath deployments); `interface/src/components/UserMenu.tsx` (MSAL `logoutRedirect` with `postLogoutRedirectUri: window.location.origin`, null-return when `accounts[0]` absent in entra_disabled mode, mounted in App.tsx AppShell header); `interface/src/hooks/useEventSource.ts` transport swap (native `EventSource` → `@microsoft/fetch-event-source` so SSE inherits the authedFetch 401-refresh-exhausted path; hook contract preserved byte-for-byte so `useLiveContext`, `Overlay`, `ConnectionBanner` required zero caller changes; exponential backoff `[1000, 2000, 4000, 8000, 16000, 30000, 30000]` clamped at `MAX_RETRY_MS=30_000`); `interface/src/auth/AuthGate.tsx` (lines 50-60: global `spacebot:auth-exhausted` window listener covers both REST + SSE, currently `console.warn` body — Phase 7 PR 5 replaces with toast + Re-sign-in CTA; do NOT add a second listener; `addEventListener` call at line 57). Phase 7 PR 1 (PR #110, 2026-04-23) added 4 SPA primitives + 14 new vitest cases: `interface/src/components/VisibilityChip.tsx` (labeled pill, 33 lines, props `{visibility: Visibility, teamName?: string}`, `Visibility = "personal" | "team" | "org"` closed union with runtime "Unknown" fallback, 5 vitest cases); `interface/src/components/VisibilityFilter.tsx` (27 lines, controlled radiogroup, `VisibilityFilterValue = "all" | Visibility` DERIVED union not hand-duplicated, 2 vitest cases); `interface/src/components/ShareResourceModal.tsx` (110 lines, `ShareSubmitArgs` DISCRIMINATED UNION for onSubmit arg — `{visibility:"team", sharedWithTeamId:string} | {visibility:"personal"|"org"}` — makes `{personal + teamId}` unrepresentable at the type level; error narrowing rethrows non-API errors for the React error boundary; `aria-describedby` wires into `resourceType + resourceId`; 4 vitest cases including rejection path + programmer-error rethrow); `interface/src/auth/useMe.ts` gained `useMyPrincipalKey(): string | null` appended beneath `useMe`/`useRole` per D27 (2 vitest cases including empty-string contract pin). Interface suite now totals 61 tests across 10 files (authedFetch 19 + authTokenProvider 5 + AuthGate 6 + mockMsal 6 + useEventSource 6 + useMe 5 + useMyPrincipalKey 3 + VisibilityChip 5 + VisibilityFilter 2 + ShareResourceModal 4). Phase 7 PR 2 (PR #112, 2026-04-23, squash `c43e5d2`) grew the suite to 70 tests across 12 files: new `interface/src/test/renderWithProviders.tsx` full-route test harness (QueryClient + TanStack Router memory-history wrapper, `initialPath` option for future URL-state hydration tests), 3 jsdom shims added to `src/test/setup.ts` (ResizeObserver fires one synthetic entry, Element.getBoundingClientRect returns desktop rect, window.scrollTo is a no-op) to support route-level tests that depend on @tanstack/react-virtual + TanStack Router scroll restoration, `@` alias added to `vitest.config.ts` mirroring `vite.config.ts` so route tests resolve `@/components/*` imports identically in vite and vitest. New test files: `interface/src/routes/__tests__/AgentMemories.scope.test.tsx` (4 cases: chip-per-card in list view, filter triggers refetch, null-visibility renders NO chip per D36, no chips in graph view per D43), `interface/src/auth/__tests__/useTeams.test.tsx` (4 cases: happy path, empty array, non-OK surfaces error, 401 prefix contract). ShareResourceModal.test.tsx grew 4 → 5 (new null-currentVisibility unowned-state hint + refuses-submit-without-selection case). useMe.test.tsx assertion loosened from exact-string equality to prefix match after PR 2's JSON-parse catch began appending parse-error detail. Phase 7 PR 2 key SPA structural changes (see `interface/src/routes/AgentMemories.tsx`): conditional chip render `{m.visibility && <VisibilityChip visibility={m.visibility} teamName={m.team_name ?? undefined} />}` with NO `as Visibility` cast because the generated schema now types `visibility` as `Visibility | null | undefined`; `useTeams({ enabled: shareTarget !== null })` lazy-gates the teams fetch on modal open (SU-5); `currentVisibility={shareTarget.visibility ?? null}` passes null through (D36 compliance, removed the `?? "personal"` collapse); `await queryClient.invalidateQueries` inside try/catch (logs-but-does-not-rethrow) after `api.setResourceVisibility`; VisibilityFilter state persisted to URL via `useSearch({strict: false})` + `useNavigate` (TanStack Router escape hatch, no router.tsx changes needed); 5-tuple queryKey `["memories", agentId, sort, typeFilter, visibilityFilter]`. `interface/src/components/ShareResourceModal.tsx` widened `currentVisibility: Visibility | null` — renders `role="note"` hint "No visibility recorded yet. Choose one to claim ownership." with no radio pre-selected; `onConfirm` rejects null-visibility submit with "Choose a visibility." alert. `packages/api-client/src/client.ts` hand-authored `MemoryItem` + `MemoriesListResponse` DELETED (were shadowing the generated `MemoryListItem` intersection type + the `forgotten` drift-comment was already stale); re-exported via `packages/api-client/src/types.ts` as `export type MemoryItem = components["schemas"]["MemoryListItem"]`. `interface/src/auth/useMe.ts` gained `useTeams({ enabled })` hook (5-min staleTime matches useMe, matching the daemon's `group_cache_ttl_secs`). Both `useMe` and `useTeams` JSON-parse catches use STRING-CONCAT fallback `` `: ${e.message}` `` rather than `Error(msg, {cause: e})` because tsconfig target is ES2020 (2-arg Error ctor is ES2022) — the parse-error detail is still preserved, just folded into the message string.
│   ├── src/              # React app, components, routes, hooks
│   └── opencode-embed-src/  # Embeddable widget variant
├── packages/             # @spacebot/* workspace scope. Currently: api-client/ (TypeScript client for Spacebot REST API + SSE event types; codegen target for `just typegen`). Added 2026-04-19 in PR #75 as the activate-api-client-package OpenSpec change. Subpath-only exports: ./client, ./types, ./schema, and ./authedFetch (added 2026-04-23 in Phase 6 PR B, PR #108, squash `524d366`). Any future sibling here must publish under @spacebot/* to be covered by scripts/check-workspace-protocol.sh. schema.d.ts grew by ~133 lines in PR #106 for the 2 Phase-5 admin audit routes + VerifyChainResponse schema. Phase 6 PR B added `src/authedFetch.ts` (173 lines) — module-private `RetryState` + `authedFetchInner`, public `authedFetch(input, init?)` with two-budget retry (AUTH_CAP=1 / SYNC_CAP=3 / TOTAL_CAP=5), FormData + Blob Content-Type scrub, `spacebot:auth-exhausted` CustomEvent dispatch via a dedicated SSR-guarded `dispatchAuthExhausted(url, reason)` helper, exported `parseRetryAfterMs` for direct unit testing. `client.ts` post-PR-B: zero raw `fetch(` calls; 88 sites migrated to `authedFetch(`; `fetchJson` helper now routes through authedFetch and throws `API error <status>: <path>` on !ok; new type re-exports `AuthExhaustedReason` + `AuthExhaustedDetail` from `./authedFetch` for downstream listener typing. Phase 6 PR C (PR #109, 2026-04-23) grew `src/schema.d.ts` by ~62 lines for the `MeResponse` object type + the `PrincipalType` string-literal TS union `"user" | "system" | "service_principal" | "legacy_static"` emitted by the `utoipa::ToSchema` derive on the Rust enum. `@spacebot/api-client` API surface unchanged — no new subpath exports, just the authedFetch re-exports already shipped in PR B. Phase 7 PR 1.5 (PR #111, 2026-04-23, squash `901be41`) added `api.setResourceVisibility(resourceType, resourceId, args: SetResourceVisibilityArgs): Promise<void>` method to client.ts + the `SetResourceVisibilityArgs` discriminated-union type (`{visibility:"team"; sharedWithTeamId:string} | {visibility:"personal"|"org"}`) mirroring PR 1's `ShareSubmitArgs`. Helper translates camelCase → snake_case at the boundary so downstream callers pass the modal's onSubmit payload through unchanged. Throws `API error <status>: <path>` on !ok. Also bootstrapped first vitest suite in this package: new `vitest.config.ts` (jsdom env because client.ts references `window.__SPACEBOT_BASE_PATH`), added `"test"` + `"test:watch"` scripts to package.json, and 5 vitest cases in `src/__tests__/setResourceVisibility.test.ts` (team branch wire translation, personal branch explicit null, org branch explicit null, !ok throws, URL-encodes path segments). schema.d.ts grew by ~140 lines across two typegen regens in PR 1.5: SetVisibilityRequest + VisibilityTag + MemoryListItem/TaskListItem/AgentListItem intersection types + CronJobWithStats inline fields + the expanded SetVisibilityRequest description after S2's validate-method doc.
├── spaceui/              # SpaceUI design system (6 packages: tokens, primitives, forms, icons, ai, explorer). Consumed by interface/ via bun workspace protocol — interface/package.json declares "workspaces": ["../spaceui/packages/*", "../packages/*"] (symlinks both @spacedrive/* and @spacebot/*) and pins each workspace dep to "workspace:*"
├── spacedrive/           # Vendored Spacedrive platform (independent Cargo workspace, own toolchain `stable`). Now a real fork — PR #57 authored 10 stub files under core/src/ to unblock sd-server compile. SYNC.md LOCAL_STATE register is load-bearing; the fork is ours per the 2026-04-16 self-reliance decision (ownership reframe in commits 778d4c3 + 48f536b). Never bulk-rsync from any external source.
├── docs/                 # Documentation site (Next.js + Fumadocs). design-docs/entra-audit-log.md added Phase 5 (PR #106, 2026-04-22) — 230-line operator guide: audit_events table shape, chain-verification procedure (automated via /api/admin/audit/verify + manual via canonical_bytes recomputation), three export modes, retention expectations (1yr SQLite / 7yr WORM), SOC 2 CC6.6 separation-of-duties mapping.
├── desktop/              # Tauri desktop app (spacebot-desktop)
├── migrations/           # 55 SQLite migrations: 41 flat per-agent + 14 instance-wide under global/ (2026-02 → 2026-04). Phase 2 (2026-04-21) added 5 instance-wide migrations: users, teams, team_memberships, service_accounts, resource_ownership (the last is a sidecar keyed by `(resource_type, resource_id)` per research §12 Alternative-1). Phase 3 (2026-04-21) added `users_photo` (nullable display_photo_b64 + photo_updated_at for A-19). Phase 4 PR 1 + Phase 4 PR 2 added 0 migrations (pure policy enforcement on existing Phase 2 data model). Phase 5 (2026-04-22, PR #106) added `20260420120007_audit_events.sql` — hash-chained `audit_events` table (monotonic seq, prev_hash/row_hash chaining, 5 user indexes) + companion `audit_export_state` table keyed on export_mode (A-14 incremental cursor).
├── presets/              # 11 agent persona presets (each has IDENTITY.md, ROLE.md, SOUL.md, meta.toml; integration-engineer + sre added 2026-04-19 in PR #74)
├── scripts/              # 11 build & release scripts (graphify-rebuild.sh added 2026-04-21 for directed-graph builds) + scripts/_disabled/check-migration-safety.sh (relocated 2026-04-20, not wired into any gate). gate-pr.sh wires 4 guards: check-sidecar-naming, check-workspace-protocol, check-vite-dedupe, check-adr-anchors. gate-pr.sh default unit-test runner flipped to cargo-nextest 2026-04-22 (R1 from streamlining audit) — `GATE_PR_NEXTEST=0` reverts per-invocation.
├── tests/                # 43 integration test files. Phase 6 PR C (PR #109, 2026-04-23) added `api_me.rs` (6 tests for the `/api/me` endpoint: happy-path shape, photo-row-present-with-null-blob-falls-back-to-initials, service_principal_serializes_as_snake_case, entra_disabled 200, missing auth 401, principal_type PrincipalType enum variants). Phase 6 PR A (PR #107, 2026-04-23) added `api_auth_config.rs` (4 tests: endpoint reachable w/o bearer, no secrets leak in populated-config response, populated-config field projection, entra_disabled shape). Same PR added +1 test to `api_auth_middleware.rs` (static-token allowlist bypass regression) and +1 to `entra_jwt_middleware.rs` (Entra-JWT allowlist bypass regression). Phase 3 (PR #103, 2026-04-21) added graph_integration.rs covering Graph API client + OBO token exchange + group resolution, 273-line file exercising Wiremock-backed Entra fakes. Phase 4 PR 1 (PR #104, 2026-04-22) added 4 test files: policy_table.rs (15 tests for check_read/check_write decision matrix; Phase 4 PR 2 grew it to 16 with admin_reading_other_admin_resource_sets_override regression), link_policy.rs (4 tests for can_link_channel), branch_inherits_auth_context.rs (3 structural tests for AgentDeps.auth_context clone propagation; Phase 4 PR 2 grew it to 5 tests by adding InboundMessage unwrap-to-legacy-static fallback coverage), api_memories_authz.rs (7 integration tests for list_memories enforcement including pool-None no-op + 401-without-bearer + team-visibility happy path; Phase 4 PR 2 grew it to 10 tests covering search_memories + memory_graph + memory_graph_neighbors + feature-gated authz_skipped_total counter-delta assertion; Phase 5 PR #106 adds create-memory-assigns-ownership G4 test → 11 tests total). Phase 4 PR 2 (PR #105, landed 2026-04-22, squash `bb9156c`) added 10 new handler-authz test files + 5 post-review canonical-coverage additions: api_agents_authz.rs (8), api_attachments_authz.rs (5), api_cron_authz.rs (7), api_ingest_authz.rs (5), api_notifications_authz.rs (6), api_portal_authz.rs (7), api_projects_authz.rs (5), api_tasks_authz.rs (8), api_wiki_authz.rs (6), send_agent_message_authz.rs (4). Phase 5 (PR #106, 2026-04-22) added 7 new test files: `audit_chain.rs` (5 hash-chain integrity tests: first_row_zero_prev_hash, chain_linked, row_hash_deterministic, concurrent_appends_no_corruption multi_thread 20-spawn, chain_verify_detects_tamper), `audit_scrubbing.rs` (2: jwt_in_metadata_scrubbed, benign_preserved), `audit_export.rs` (3: filesystem_writes_ndjson_and_manifest, export_skipped_when_disabled, incremental_cursor_skips_already_exported_rows), `api_admin_audit.rs` (6: non_admin_403, admin_ndjson, csv Accept, verify clean, verify tampered, non_admin_verify_403), `channel_turn_deps.rs` (3 data-level restore invariants), `audit_middleware_emission.rs` (2 wire tests: valid auth emits auth_success row, invalid emits auth_failure), `audit_handler_emission.rs` (2 wire tests: admin break-glass emits admin_read row, non-owner denied emits authz_denied row). Plus 5 unit tests added to `src/api/audit.rs::tests` (csv_escape RFC-4180 quoting + OWASP formula-injection guard, accept_prefers_csv first-token-wins). Phase 0 auth hardening (PR #81, 2026-04-20, commit `2a40131`) added `api_auth_middleware.rs` (11 tests) and `leak_detection_jwt.rs` (3 tests). Phase 1 (PR #82, 2026-04-20, commit `91b8e38`) added `entra_jwt_middleware.rs` (16 tests) and `tests/support/mock_entra.rs`. Phase 2 (PR #101, 2026-04-21, commit `bd7579b`) added `authz_data_model.rs` (12 tests). `spacedrive_client.rs` carries wiremock-backed RPC envelope + 401/Bearer round-trip tests; `litellm_proxy.rs` added 2026-04-19 in PR #77. Phase 7 PR 1.5 (PR #111, 2026-04-23) added `api_resource_visibility.rs` (8 tests: owner_can_change_visibility_personal_to_team, non_owner_cannot_change_visibility, admin_can_change_any_visibility, team_visibility_without_team_id_rejected, rotation_preserves_owner_agent_id [C1 regression pin], owner_can_rebind_team_id, unauthenticated_returns_401, invalid_visibility_value_rejected). Also added 5 new tests to `authz_data_model.rs` in PR 1 (see auth/ subtree entry above, 12 → 17). Phase 7 PR 2 (PR #112, 2026-04-23, squash `c43e5d2`) added `tests/api_teams.rs` (8 tests: authenticated_user_gets_active_teams_sorted_by_display_name, unauthenticated_returns_401, archived_teams_are_filtered, empty_teams_table_returns_empty_array, user_without_spacebot_user_role_is_forbidden, service_principal_without_spacebot_user_role_is_forbidden, admin_with_spacebot_user_role_grants_access, same_display_name_sort_is_stable_by_id).
├── vendor/               # Vendored crate: imap-proto-0.10.2
├── nix/                  # Nix build support
├── flake.nix             # Nix flake definition
└── justfile              # Task runner recipes
```

## Module File Convention
Never create `mod.rs` files. Use `src/memory.rs` as the module root (not `src/memory/mod.rs`).
The module root file contains `mod` declarations and re-exports.
